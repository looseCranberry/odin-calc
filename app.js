let memory = 0;
let input = null;

const buttons = document.querySelectorAll("button");
buttons.forEach((button) => {
	onClick(button.id);
});

function opHandler(operator) {}
function numHandler(number) {}
function memHandler(button) {}
